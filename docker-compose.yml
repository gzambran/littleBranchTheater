services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3003:3003"
    stdin_open: true
    tty: true
    volumes:
      - ./public:/app/public
    restart: unless-stopped