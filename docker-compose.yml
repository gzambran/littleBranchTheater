services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: little-branch-site
    ports:
      - "3001:3003"
    stdin_open: true
    tty: true
    volumes:
      - ./public:/app/public
    restart: unless-stopped
